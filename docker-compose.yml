services:
  rdr:
    image: ghcr.io/chrisallenlane/rdr:latest
    ports:
      - "8080:8080"
    volumes:
      - rdr-data:/data
    environment:
      - RDR_LISTEN_ADDR=:8080
      - RDR_POLL_INTERVAL=6h
      # - RDR_RETENTION_DAYS=90
    restart: unless-stopped

volumes:
  rdr-data:
